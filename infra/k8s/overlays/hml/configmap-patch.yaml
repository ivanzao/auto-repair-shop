apiVersion: v1
kind: ConfigMap
metadata:
  name: auto-repair-shop-config
data:
  APPLICATION_PROFILE: "hml"
  # DATABASE_URL, DATABASE_USERNAME, SNS_TOPIC_ARN são injetados em runtime pelo CI (vêm de SSM)
