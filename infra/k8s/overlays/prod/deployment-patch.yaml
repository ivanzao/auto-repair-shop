apiVersion: apps/v1
kind: Deployment
metadata:
  name: order
spec:
  replicas: 2
  template:
    spec:
      containers:
        - name: order
          resources:
            requests:
              cpu: 500m
              memory: 768Mi
            limits:
              memory: 1.5Gi
