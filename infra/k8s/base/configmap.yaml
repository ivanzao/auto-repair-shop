apiVersion: v1
kind: ConfigMap
metadata:
  name: order-config
data:
  SERVER_PORT: "8080"
  # DATABASE_URL, DATABASE_USERNAME, SNS_TOPIC_ARN (tópico order-events) e SQS_QUEUE_URL
  # (fila de entrada do order) são patched no overlay em runtime (lidos de SSM no CI).
