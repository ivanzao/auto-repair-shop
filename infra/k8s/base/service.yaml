apiVersion: v1
kind: Service
metadata:
  name: auto-repair-shop-service
spec:
  type: NodePort
  selector:
    app: auto-repair-shop
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30080
      protocol: TCP
      name: http
