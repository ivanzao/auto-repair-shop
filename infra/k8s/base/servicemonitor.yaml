apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: order
  labels:
    release: prom
spec:
  selector:
    matchLabels:
      app: order
  endpoints:
    - port: http
      path: /metrics
      interval: 30s
