application:
  profile: test

database:
  maximumPoolSize: 6

security:
  hash:
    cost: 4  # Lower cost for faster tests

sns:
  topic:
    arn: placeholder
aws:
  region: us-east-1
  endpoint: placeholder
  accessKeyId: test
  secretAccessKey: test
