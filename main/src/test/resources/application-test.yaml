application:
  profile: test

database:
  maximumPoolSize: 6

security:
  hash:
    cost: 4  # Lower cost for faster tests

sns:
  topic:
    arn: __injected-by-testcontainers__
sqs:
  queue:
    url: __injected-by-testcontainers__
aws:
  region: us-east-1
  endpoint: __injected-by-testcontainers__
  accessKeyId: test
  secretAccessKey: test
