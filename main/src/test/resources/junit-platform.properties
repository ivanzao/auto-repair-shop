# Cucumber roda apenas via a suíte @Suite (OrderFlowBddIntegrationTest), não como
# root engine: evita executar no task `test` (sem Docker) e execução dupla.
cucumber.junit-platform.discovery.as-root-engine=false
cucumber.plugin=pretty
