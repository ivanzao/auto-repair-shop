application:
  url: "${APPLICATION_URL:http://localhost:8080}"
  profile: "${APPLICATION_PROFILE:dev}"

server:
  port: "${SERVER_PORT:8080}"

database:
  driverClassName: org.postgresql.Driver
  url: "${DATABASE_URL:jdbc:postgresql://localhost:5432/auto-repair-shop}"
  username: "${DATABASE_USERNAME:app}"
  password: "${DATABASE_PASSWORD:test}"
  maximumPoolSize: "${DATABASE_MAX_POOL_SIZE:6}"

security:
  hash:
    cost: "${SECURITY_HASH_COST:10}"  # BCrypt cost factor (10-12 recommended for production)

sns:
  topic:
    # tópico de eventos do order (produz OrderCreated). Sempre setado por compose/k8s (SNS_TOPIC_ARN).
    arn: "${SNS_TOPIC_ARN}"

sqs:
  queue:
    # fila de eventos de entrada do order. Sempre setado por compose/k8s (SQS_QUEUE_URL).
    url: "${SQS_QUEUE_URL}"

aws:
  region: "${AWS_REGION:us-east-1}"
  endpoint: "${AWS_ENDPOINT:}"
  accessKeyId: "${AWS_ACCESS_KEY_ID:}"
  secretAccessKey: "${AWS_SECRET_ACCESS_KEY:}"
